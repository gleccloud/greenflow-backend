# API Console 구조도

## 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    GreenFlow API Console                         │
│                    (World-class Enterprise)                      │
└─────────────────────────────────────────────────────────────────┘

                          ┌─────────────┐
                          │ /console    │ (Protected Route)
                          └──────┬──────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        │                        │                        │
    ┌───▼────────┐      ┌────────▼─────────┐      ┌──────▼──────┐
    │ ConsoleLayout                         │      │ ConsoleContext
    │ ─────────────────────────────         │      │ ──────────────
    │ • Sidebar (접이식)              │      │ • User 정보
    │ • Navigation (8개 탭)              │      │ • Settings
    │ • Header (현재 페이지)             │      │ • Loading 상태
    │ • User Menu                         │      │ • Notifications
    └────────────┬────────────────────────┘      └──────────────┘
                 │
                 │
    ┌────────────┴────────────┬─────────────┬──────────────┬────────────────┬─────────────┬───────────┐
    │                         │             │              │                │             │           │
┌───▼────────┐  ┌────────────▼──┐  ┌──────▼──┐  ┌────────▼──────┐  ┌────────▼──┐  ┌──────▼───┐  ┌──▼───────┐
│ Dashboard  │  │   API Keys    │  │  Logs   │  │Documentation │  │ Webhooks  │  │Integrations│ │ Billing  │
├────────────┤  ├───────────────┤  ├─────────┤  ├───────────────┤  ├───────────┤  ├────────────┤ ├──────────┤
│ • Metrics  │  │ • CRUD Ops    │  │ • Stream│  │ • Swagger UI  │  │ • Manage  │  │ • Slack   │ │ • Plans  │
│ • Charts   │  │ • Rotation    │  │ • Filter│  │ • Examples    │  │ • Test    │  │ • Zapier  │ │ • History│
│ • Activity │  │ • IP Whitelist│  │ • Export│  │ • Code Samples│  │ • Delivery│  │ • GitHub  │ │ • Invoice│
└────────────┘  └───────────────┘  └─────────┘  └───────────────┘  └───────────┘  └────────────┘ └──────────┘

```

## 계층별 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                     UI Layer (Pages)                             │
│  Dashboard | APIKeys | Logs | Documentation | Webhooks | ...    │
└────────────────────────┬────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                  Custom Hooks Layer                              │
│  useAPIKeys() | useLogs() | useMetrics() | useWebhooks()        │
│  ─────────────────────────────────────────────────────────────  │
│  • State Management    • Pagination   • Filtering               │
│  • API Calls          • Error Handling   • Real-time Subscribe   │
└────────────────────────┬────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                   Services Layer                                 │
│  apiKeyService | logsService | webhookService                   │
│  ─────────────────────────────────────────────────────────────  │
│  • CRUD Operations   • Data Transformation   • SSE Streaming     │
└────────────────────────┬────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                  API Client Layer                                │
│  apiClient (REST HTTP Client)                                   │
│  ─────────────────────────────────────────────────────────────  │
│  • Auth Token Injection   • Error Handling   • URL Building      │
└────────────────────────┬────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────────┐
│              Backend REST API (/api/v2/console/*)                │
│  • API Keys Management    • Logs Streaming    • Webhooks        │
│  • Billing Information    • Settings          • Integrations    │
└─────────────────────────────────────────────────────────────────┘
```

## Type System 구조

```
┌──────────────────────────────────────────────────────────────┐
│                   API Key Types                              │
├──────────────────────────────────────────────────────────────┤
│ APIKey                                                       │
│ ├── id: string                                              │
│ ├── name: string                                            │
│ ├── keyPrefix: string (glec_prod_...)                       │
│ ├── status: 'ACTIVE' | 'REVOKED' | 'EXPIRED'               │
│ ├── scopes: APIScope[]                                      │
│ ├── rateLimit: RateLimit                                    │
│ └── ipWhitelist: string[]                                   │
│                                                             │
│ APIScope                                                    │
│ ├── id: string                                              │
│ ├── name: string (e.g., "bids.read")                        │
│ ├── methods: HTTPMethod[]  ('GET'|'POST'|...)              │
│ └── granted: boolean                                        │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                   Log Types                                  │
├──────────────────────────────────────────────────────────────┤
│ APILog                                                       │
│ ├── id: string                                              │
│ ├── timestamp: string (ISO 8601)                            │
│ ├── method: string ('GET'|'POST'|...)                       │
│ ├── endpoint: string (/api/v2/bids)                         │
│ ├── status: 'SUCCESS' | 'FAILED' | 'PENDING'               │
│ ├── statusCode: number (200, 404, 500)                      │
│ ├── duration: number (ms)                                   │
│ └── errorMessage?: string                                   │
│                                                             │
│ LogFilter                                                   │
│ ├── startDate: string                                       │
│ ├── endDate: string                                         │
│ ├── status?: LogStatus                                      │
│ └── searchTerm?: string                                     │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                  Webhook Types                               │
├──────────────────────────────────────────────────────────────┤
│ Webhook                                                      │
│ ├── id: string                                              │
│ ├── name: string                                            │
│ ├── url: string                                             │
│ ├── events: WebhookEvent[]                                  │
│ ├── status: 'ACTIVE' | 'INACTIVE'                           │
│ └── retryPolicy: RetryPolicy                                │
│                                                             │
│ WebhookEvent (Union Type)                                   │
│ ├── 'bid.created'    │ 'bid.updated'    │ 'bid.closed'     │
│ ├── 'proposal.created'│ 'proposal.awarded'                  │
│ ├── 'order.created'  │ 'order.shipped'  │ 'order.delivered'│
│ └── 'fleet.updated'  │ 'api_key.rotated'                    │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                  Metrics Types                               │
├──────────────────────────────────────────────────────────────┤
│ UsageMetrics                                                 │
│ ├── apiKeyId: string                                        │
│ ├── date: string (YYYY-MM-DD)                               │
│ ├── requestCount: number                                    │
│ ├── successCount: number                                    │
│ ├── totalDuration: number (ms)                              │
│ └── topEndpoint: string                                     │
│                                                             │
│ MetricsSummary                                              │
│ ├── period: 'DAY' | 'WEEK' | 'MONTH'                        │
│ ├── totalRequests: number                                   │
│ ├── successRate: number (0-100)                             │
│ ├── averageResponseTime: number                             │
│ └── peakRequestsPerSecond: number                           │
└──────────────────────────────────────────────────────────────┘
```

## State Management Flow

```
┌──────────────────────────────────────────────────────────────┐
│               Component / Page (Dashboard.tsx)                │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ useAPIKeys()
                         ▼
┌──────────────────────────────────────────────────────────────┐
│         Custom Hook (useAPIKeys)                              │
│ ─────────────────────────────────────────────────────────────│
│ const [keys, setKeys] = useState()                            │
│ const [page, setPage] = useState()                            │
│ const [error, setError] = useState()                          │
│                                                              │
│ const fetchKeys = async (page) => {                           │
│   const result = await apiKeyService.listAPIKeys()           │
│   setKeys(result.keys)                                       │
│ }                                                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ apiKeyService.listAPIKeys()
                         ▼
┌──────────────────────────────────────────────────────────────┐
│         Service Layer (apiKeyService)                         │
│ ─────────────────────────────────────────────────────────────│
│ export const apiKeyService = {                                │
│   listAPIKeys: async (page, pageSize) => {                   │
│     return await apiClient.get('/console/api-keys', ...)    │
│   }                                                          │
│ }                                                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ apiClient.get()
                         ▼
┌──────────────────────────────────────────────────────────────┐
│         API Client (apiClient)                                │
│ ─────────────────────────────────────────────────────────────│
│ class ApiClient {                                             │
│   async request(endpoint, options) {                          │
│     const token = localStorage.getItem('auth_token')          │
│     return fetch(url, {                                       │
│       headers: { Authorization: `Bearer ${token}` }          │
│     })                                                       │
│   }                                                          │
│ }                                                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ fetch() HTTP Request
                         ▼
┌──────────────────────────────────────────────────────────────┐
│         Backend API Server                                    │
│ GET /api/v2/console/api-keys?page=1&pageSize=20             │
│                                                              │
│ Response:                                                    │
│ {                                                            │
│   keys: [...],                                               │
│   total: 5,                                                  │
│   page: 1,                                                   │
│   pageSize: 20                                               │
│ }                                                            │
└──────────────────────────────────────────────────────────────┘
```

## 파일 조직

```
src/console/
│
├── types/                    # TypeScript 타입 정의 (5개)
│   ├── apiKey.ts
│   ├── logs.ts
│   ├── webhook.ts
│   ├── metrics.ts
│   └── index.ts              # 중앙 export
│
├── services/                 # 비즈니스 로직 & API (3개)
│   ├── apiClient.ts          # HTTP 클라이언트
│   ├── apiKeyService.ts      # API 키 CRUD
│   └── logsService.ts        # 로그 조회 & SSE
│
├── hooks/                    # Custom Hooks (2개)
│   ├── useAPIKeys.ts         # API 키 상태 관리
│   ├── useLogs.ts            # 로그 상태 관리
│   └── index.ts              # export
│
├── context/                  # 전역 상태 (1개)
│   └── ConsoleContext.tsx    # User, Settings, Notifications
│
├── components/               # UI 컴포넌트 (1개)
│   └── ConsoleLayout.tsx     # 메인 레이아웃
│
└── pages/                    # 페이지 (8개)
    ├── Dashboard.tsx
    ├── APIKeys.tsx
    ├── Documentation.tsx
    ├── Logs.tsx
    ├── Webhooks.tsx
    ├── Integrations.tsx
    ├── Billing.tsx
    └── Settings.tsx
```

## 라우팅 맵

```
┌─ /console (ConsoleLayout)
│  ├─ / (Dashboard)
│  ├─ /api-keys (APIKeys)
│  ├─ /documentation (Documentation)
│  ├─ /logs (Logs)
│  ├─ /webhooks (Webhooks)
│  ├─ /integrations (Integrations)
│  ├─ /billing (Billing)
│  └─ /settings (Settings)
│
├─ / (Landing Layout)
│  ├─ / (Gate)
│  ├─ /shipper (Shipper Landing)
│  ├─ /carrier (Carrier Landing)
│  └─ /owner (Owner Landing)
│
├─ /dashboard (Dashboard Layout)
│  ├─ /shipper (Shipper Dashboard)
│  ├─ /carrier (Carrier Dashboard)
│  └─ /owner (Owner Dashboard)
│
├─ /login (Login Page)
└─ /register (Register Page)
```

## 디자인 시스템

```
┌────────────────────────────────────────────────────────┐
│                   Color Palette                        │
├────────────────────────────────────────────────────────┤
│ Primary    ● emerald-600  #059669                       │
│ Success    ● emerald-600  #059669                       │
│ Error      ● red-600      #dc2626                       │
│ Warning    ● orange-600   #ea580c                       │
│ Info       ● blue-600     #2563eb                       │
│ Neutral    ● slate-600    #475569                       │
│ Background ● slate-50     #f8fafc                       │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                   Components                           │
├────────────────────────────────────────────────────────┤
│ Cards       rounded-xl shadow-sm border border-slate-200
│ Buttons     rounded-lg hover:transition-colors
│ Inputs      rounded-lg border focus:ring-emerald-500
│ Tables      border-collapse hover:bg-slate-50
│ Badges      px-3 py-1 rounded-full text-xs font-medium
│ Sidebars    w-64 (desktop) / drawer (mobile)
└────────────────────────────────────────────────────────┘
```

---

이 구조도는 GreenFlow API Console의 전체 설계를 시각화합니다.
모든 파일이 생성되었고 빌드가 성공했습니다. Phase 2에서는 실제 API 연결을 하게 됩니다. 🚀
